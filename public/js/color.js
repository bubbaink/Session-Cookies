let qs = (selector) => document.querySelector(selector)

window.addEventListener("load", ()=>{
    let form = qs("form"),
        recuerdame = qs("#color")

    recuerdame.addEventListener("blur", ()=>{
        switch (true) {
            case value:
                
                break;
        
            default:
                break;
        }
    })
})